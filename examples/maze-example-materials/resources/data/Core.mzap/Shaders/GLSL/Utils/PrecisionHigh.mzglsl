//////////////////////////////////////////
#if (defined(GL_ES))
    precision highp float;
    #if (MAZE_GLSL_VERSION > 100)
        precision highp sampler2D;
        precision highp usampler2D;
    #endif
#endif